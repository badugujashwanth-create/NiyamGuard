# Social preview upload

`social-preview.png` is the prepared 1280×640 repository preview.

GitHub does not expose social-preview upload through the standard `gh repo edit` flow. After the release PR merges, open **Repository → Settings → General → Social preview → Edit**, upload this PNG, save, and confirm the preview is readable in the sharing card. Do not substitute an unverified marketing claim.
