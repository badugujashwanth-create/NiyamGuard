# Demo verification evidence

The seven frames sample the accepted video at 0:10, 0:30, 1:00, 1:40, 2:40, 3:50, and 4:10. They cover the product boundary, guided overview, successful simulation, generated evidence, drift remediation, public certificate verification, and authenticated audit log.

`verification.json` records duration, resolution, codecs, byte size, caption path, frame count, and SHA-256 for `../demo.webm`. The video is accepted only after the browser simulation passes and media checks confirm a duration of at least three minutes, 1280×720 video, and an audio stream.
